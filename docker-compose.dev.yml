services:
  hstplus-site:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: hstplus-site-dev
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
